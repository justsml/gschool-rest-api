{
  "name": "gschool-rest-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "json-server http://jsonplaceholder.typicode.com/db --host $APP_HOST --port $PORT --snapshots ./data ",
    "dev": "PORT=5000 APP_HOST=localhost npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
